#/rtc/livekit.yaml
port: 7880

# Optional — you can also delete this block entirely.
bind_addresses:
  - "0.0.0.0"

rtc:
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: false

redis:
  address: ""
  username: ""
  password: ""

keys:
  carebell: ${LIVEKIT_API_SECRET}

turn:
  enabled: false

webhook:
  urls: []
